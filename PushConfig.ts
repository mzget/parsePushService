class PushConfig {
    pushServer: string;
    ParseApplicationId : string;
    ParseRESTAPIKey: string;
    ParseMasterKey: string;

    pushPort: number;
    pushPath: string;
}

module.exports = PushConfig;